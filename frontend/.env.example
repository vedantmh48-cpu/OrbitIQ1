# Local development: leave blank to use the Vite proxy to localhost:8000.
# Production (Vercel): set to the backend origin only, with no /api suffix.
VITE_API_BASE=
# Example format (replace with your real backend hostname):
# VITE_API_BASE=https://YOUR_BACKEND_DOMAIN
